# Frontend Apache Module

This module will host the asynchronous HTML+JavaScript client served by Apache over HTTPS.

## Planned Responsibilities

- Serve static frontend assets.
- Enforce HTTPS access.
- Apply secure HTTP headers.
- Route async requests to the Spring backend endpoint.

Implementation starts in a later phase (see `docs/roadmap.md`).

